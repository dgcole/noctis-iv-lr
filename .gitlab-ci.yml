image: debian:buster-slim

before_script:
  - apt-get update -qq && apt-get install -y -qq clang cmake make

stages:
  - build

build:
  stage: build
  script: "mkdir build && cd build && cmake .. && make && mv nivlr ../"
  artifacts:
    paths:
      - nivlr
      - res/supports.nct
